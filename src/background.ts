// import { createAction } from './action';
// import { EventName } from './types';

// chrome.action.onClicked.addListener((tab) => {
//   chrome.tabs
//     .sendMessage(tab.id, createAction(EventName.StartParse, ''))
//     .then((response) => {
//       chrome.tabs.create({});
//     });
// });
